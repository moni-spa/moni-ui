/**
 * Complete browser distribution for CDN and no-bundler usage.
 * Registers every Moni UI custom element. Load `moni-ui.min.css` alongside it
 * when the global tokens and utility styles are needed.
 */
export * from './web-components.js';
